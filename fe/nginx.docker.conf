# ZenithPark FE SPA (컨테이너 내부 전용 — 호스트 nginx 설정과 무관)
server {
    listen 80;
    listen [::]:80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
}
